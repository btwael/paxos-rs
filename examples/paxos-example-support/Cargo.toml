[package]
name = "paxos-example-support"
version = "0.1.0"
edition = "2018"

[dependencies]
bincode = "1.3"
bytes = { version = "1.11.1", features = ["serde"] }
paxos = { path = "../.." }
serde = { version = "1.0", features = ["derive"] }
traceforge-rounds = { path = "../../../TraceForge/traceforge-rounds" }
